# Claude Prompt Template (Copy/Paste)

Context:
- Repo: linux-security-log-analyzer
- Goal: (what you want)
- Constraints:
  - minimal changes
  - no new dependencies unless required
  - keep behavior the same unless explicitly requested
- Definition of done:
  - script runs
  - tests pass: python -m pytest -q
  - git diff reviewed
  - docs updated if needed

Task:
(Describe ONE small task)

Before editing:
1) summarize current behavior
2) propose a step-by-step plan
3) list risks + verification commands

Then:
- provide the smallest patch only
